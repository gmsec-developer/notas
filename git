LINEAS DE PARAMETRIZACION Y USO DE GIT

git --version       // Verificar la version de git

// Registro de usuario
git config --global user.name "GMSec Developer"         //Definicon de usuario
git config --global user.email soporte.gmsec@gmail.com  //Definicion de email
git config --global core.editor "code --wait"           //Espera a que el terminal se cierre
git config --global -e                                  //Archivos de configuración global
git config --global core.autocrlt true                  //true->windows   input->mac Configuracion del salto de linea
git config -h                                           //Listado de opciones de Configuracion

// Manejo de Archivos
ls                              //Listado del contenido de un directorio
ls -a                           //Todos los archivos ocultos
pwd                             //Directorio actual
cd                              //Entrar a directorio
cd ..                           //Salir de directorio
mkdir miweb                     //Crear directorio
mv archivo.ext archivo1.ext     //Renombra un archivo moviendolo


//////////////////////////////////////////////////////

git init                        //Inicializar un proyecto, en la carpeta actual
code .                          //Abre el proyecto en VSC
git status                      //Muestra el estado de los cambios
git status -s                   //Muestra el estado en resumen
git add nombrearchivo.ext       //Añade los cambios al area de stage
git commit -m "Descripcion"     //Compromete o Commitea los cambios antes de cargar al server

Sincronizar con GitHub
git branch -M main              //Define la rama donde se esta trabajando
git remote add origin https://github.com/gmsec-developer/notas.git      //Dirección de GitHub
git push -u origin main         // Envia los archivos locales al repositorio de GitHub

